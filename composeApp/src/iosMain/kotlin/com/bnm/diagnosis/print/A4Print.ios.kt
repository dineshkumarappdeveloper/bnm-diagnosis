package com.bnm.diagnosis.print

/** Staged, same as the family's PDF printing — thermal (AirPrint via the
 *  system path) covers iOS counters meanwhile. */
actual fun printA4(doc: A4Doc): String = "A4 printing arrives on iOS later — use Thermal for now"
